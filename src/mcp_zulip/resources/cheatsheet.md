# Zulip `zulip_call_endpoint` quick reference

Duplicate of the table in `zulip://api-guide` for agents that fetch templates by URI.

| method | url |
|--------|-----|
| GET | messages |
| POST | messages |
| PATCH | messages/{id} |
| DELETE | messages/{id} |
| POST | messages/flags |
| POST | mark_all_as_read |
| GET | users |
| GET | streams |
| GET | users/me/subscriptions |
| POST | users/me/subscriptions |

Full context and links: fetch resource `zulip://api-guide`.
